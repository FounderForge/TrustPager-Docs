// AUTOGENERATED by scripts/sync-from-api.ts — DO NOT EDIT BY HAND.
// Source of truth: ../../../TrustPager-Portal/supabase/functions/api/routes/whatsapp.ts
// To regenerate after API changes: npm run sync:api
// Hand-curated examples live in scripts/api-sync-examples/whatsapp.json

import { type ResourceGroup, API_BASE_URL } from './types.js';

export const WHATSAPP: ResourceGroup = {
  id: 'whatsapp',
  label: 'WhatsApp',
  description: 'Send and receive WhatsApp messages. Pair a phone number, view 1:1 conversations and group conversations, send messages, and manage CRM links.',
  endpoints: [
    {
      method: 'GET',
      path: '/whatsapp/conversations',
      toolName: 'list_whatsapp_conversations',
      description: 'List WhatsApp 1:1 conversations in the workspace inbox. Supports filtering by external_phone_number and unread.',
      scopes: ['whatsapp:read'],
      isWrite: false,
      params: [
        { name: 'limit', type: 'number', required: false, description: '', in: 'query' },
        { name: 'after', type: 'string', required: false, description: '', in: 'query' },
        { name: 'external_phone_number', type: 'string', required: false, description: '', in: 'query' },
        { name: 'unread', type: 'boolean', required: false, description: '', in: 'query' },
      ],
      requestExample: `curl \
  "${API_BASE_URL}/whatsapp/conversations" \
  -H "Authorization: Bearer YOUR_API_KEY"`,
    },
    {
      method: 'GET',
      path: '/whatsapp/conversations/:conversation_id',
      toolName: 'get_whatsapp_conversation',
      description: 'Fetch a single WhatsApp conversation by UUID.',
      scopes: ['whatsapp:read'],
      isWrite: false,
      params: [
        { name: 'conversation_id', type: 'string', required: true, description: '', in: 'path' },
      ],
      requestExample: `curl \
  "${API_BASE_URL}/whatsapp/conversations/:conversation_id" \
  -H "Authorization: Bearer YOUR_API_KEY"`,
    },
    {
      method: 'PATCH',
      path: '/whatsapp/conversations/:conversation_id',
      toolName: 'update_whatsapp_conversation',
      description: 'Update a WhatsApp conversation (e.g. mark read, archive).',
      scopes: ['whatsapp:write'],
      isWrite: true,
      params: [
        { name: 'conversation_id', type: 'string', required: true, description: '', in: 'path' },
        { name: 'unread_count', type: 'number', required: false, description: '', in: 'body' },
        { name: 'status', type: 'string', required: false, description: '', in: 'body' },
      ],
      requestExample: `curl -X PATCH \
  "${API_BASE_URL}/whatsapp/conversations/:conversation_id" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"unread_count":"...","status":"..."}'`,
    },
    {
      method: 'GET',
      path: '/whatsapp/conversations/:conversation_id/messages',
      toolName: 'get_whatsapp_messages',
      description: 'List all messages in a WhatsApp conversation in chronological order.',
      scopes: ['whatsapp:read'],
      isWrite: false,
      params: [
        { name: 'conversation_id', type: 'string', required: true, description: '', in: 'path' },
      ],
      requestExample: `curl \
  "${API_BASE_URL}/whatsapp/conversations/:conversation_id/messages" \
  -H "Authorization: Bearer YOUR_API_KEY"`,
    },
    {
      method: 'POST',
      path: '/whatsapp/send',
      toolName: 'send_whatsapp',
      description: 'Send a WhatsApp message to a contact. Provide either to_phone (E.164) or conversation_id. Costs credits.',
      scopes: ['whatsapp:send'],
      isWrite: true,
      params: [
        { name: 'to_phone', type: 'string', required: false, description: 'Recipient phone in E.164, e.g. +61431377068', in: 'body' },
        { name: 'body', type: 'string', required: true, description: 'WhatsApp message body (text only in v1).', in: 'body' },
        { name: 'conversation_id', type: 'string', required: false, description: 'Existing conversation UUID. Mutually exclusive with to_phone.', in: 'body' },
        { name: 'contact_id', type: 'string', required: false, description: '', in: 'body' },
        { name: 'deal_id', type: 'string', required: false, description: '', in: 'body' },
        { name: 'customer_id', type: 'string', required: false, description: '', in: 'body' },
      ],
      requestExample: `curl -X POST \
  "${API_BASE_URL}/whatsapp/send" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"to_phone":"...","body":"...","conversation_id":"..."}'`,
      responseExample: `{
  "data": {
    "success": true,
    "whatsapp_message_id": "uuid...",
    "conversation_id": "uuid...",
    "status": "queued"
  }
}`,
    },
    {
      method: 'POST',
      path: '/whatsapp/groups/send',
      toolName: 'send_whatsapp_group',
      description: 'Send a WhatsApp message into a registered group. Group must be visible to the workspace (paired account is a member). Costs credits.',
      scopes: ['whatsapp:send'],
      isWrite: true,
      params: [
        { name: 'group_id', type: 'string', required: true, description: 'TrustPager whatsapp_groups UUID (not the WhatsApp JID).', in: 'body' },
        { name: 'body', type: 'string', required: true, description: 'Message body (text only in v1).', in: 'body' },
      ],
      requestExample: `curl -X POST \
  "${API_BASE_URL}/whatsapp/groups/send" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"group_id":"...","body":"..."}'`,
    },
    {
      method: 'GET',
      path: '/whatsapp/groups',
      toolName: 'list_whatsapp_groups',
      description: 'List WhatsApp groups the workspace\'s paired account is in. Filter by linked_deal_id, linked_customer_id, or unlinked.',
      scopes: ['whatsapp:read'],
      isWrite: false,
      params: [
        { name: 'limit', type: 'number', required: false, description: '', in: 'query' },
        { name: 'after', type: 'string', required: false, description: '', in: 'query' },
        { name: 'linked_deal_id', type: 'string', required: false, description: '', in: 'query' },
        { name: 'linked_customer_id', type: 'string', required: false, description: '', in: 'query' },
        { name: 'linked', type: 'boolean', required: false, description: 'true = only linked groups, false = only unlinked', in: 'query' },
      ],
      requestExample: `curl \
  "${API_BASE_URL}/whatsapp/groups" \
  -H "Authorization: Bearer YOUR_API_KEY"`,
    },
    {
      method: 'GET',
      path: '/whatsapp/groups/:group_id',
      toolName: 'get_whatsapp_group',
      description: 'Fetch a single WhatsApp group by UUID, including member list snapshot.',
      scopes: ['whatsapp:read'],
      isWrite: false,
      params: [
        { name: 'group_id', type: 'string', required: true, description: '', in: 'path' },
      ],
      requestExample: `curl \
  "${API_BASE_URL}/whatsapp/groups/:group_id" \
  -H "Authorization: Bearer YOUR_API_KEY"`,
    },
    {
      method: 'GET',
      path: '/whatsapp/groups/:group_id/messages',
      toolName: 'get_whatsapp_group_messages',
      description: 'List all messages in a WhatsApp group in chronological order.',
      scopes: ['whatsapp:read'],
      isWrite: false,
      params: [
        { name: 'group_id', type: 'string', required: true, description: '', in: 'path' },
      ],
      requestExample: `curl \
  "${API_BASE_URL}/whatsapp/groups/:group_id/messages" \
  -H "Authorization: Bearer YOUR_API_KEY"`,
    },
    {
      method: 'POST',
      path: '/whatsapp/groups/:group_id/link',
      toolName: 'link_whatsapp_group',
      description: 'Link a WhatsApp group to a deal or customer so its messages appear in the deal/customer timeline.',
      scopes: ['whatsapp:write'],
      isWrite: true,
      params: [
        { name: 'group_id', type: 'string', required: true, description: '', in: 'path' },
        { name: 'deal_id', type: 'string', required: false, description: '', in: 'body' },
        { name: 'customer_id', type: 'string', required: false, description: '', in: 'body' },
      ],
      requestExample: `curl -X POST \
  "${API_BASE_URL}/whatsapp/groups/:group_id/link" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"deal_id":"...","customer_id":"..."}'`,
    },
    {
      method: 'POST',
      path: '/whatsapp/groups/:group_id/unlink',
      toolName: 'unlink_whatsapp_group',
      description: 'Remove a WhatsApp group\'s deal/customer link. The group remains visible in the inbox.',
      scopes: ['whatsapp:write'],
      isWrite: true,
      params: [
        { name: 'group_id', type: 'string', required: true, description: '', in: 'path' },
      ],
      requestExample: `curl -X POST \
  "${API_BASE_URL}/whatsapp/groups/:group_id/unlink" \
  -H "Authorization: Bearer YOUR_API_KEY"`,
    },
    {
      method: 'GET',
      path: '/whatsapp/pairing',
      toolName: 'get_whatsapp_pairing_status',
      description: 'Get the workspace\'s WhatsApp pairing status (connected / awaiting_qr / disconnected / etc.) and paired phone identity.',
      scopes: ['whatsapp:read'],
      isWrite: false,
      requestExample: `curl \
  "${API_BASE_URL}/whatsapp/pairing" \
  -H "Authorization: Bearer YOUR_API_KEY"`,
      responseExample: `// Connected pairing
{
  "data": {
    "pairing": {
      "id": "uuid...",
      "status": "connected",
      "paired_phone_e164": "+61412345678",
      "paired_display_name": "Simon",
      "paired_at": "2026-05-28T10:00:00Z",
      "synced_company_ids": ["ebeff86e-..."]
    }
  }
}

// No pairing active
{ "data": { "pairing": null } }`,
    },
    {
      method: 'POST',
      path: '/whatsapp/pair',
      toolName: 'start_whatsapp_pair',
      description: 'Begin pairing a WhatsApp account to the workspace. Returns a QR code data URL the user scans with their phone (Settings → Linked Devices → Link a Device). The pairing becomes "connected" automatically once they scan.',
      scopes: ['whatsapp:write'],
      isWrite: true,
      requestExample: `curl -X POST \
  "${API_BASE_URL}/whatsapp/pair" \
  -H "Authorization: Bearer YOUR_API_KEY"`,
      responseExample: `{
  "data": {
    "success": true,
    "pairing_id": "uuid...",
    "qr_code_data_url": "data:image/png;base64,...",
    "pairing_code": "ABCD-EFGH",
    "status": "awaiting_qr",
    "qr_expires_at": "2026-05-28T10:01:00Z"
  }
}`,
    },
    {
      method: 'POST',
      path: '/whatsapp/disconnect',
      toolName: 'disconnect_whatsapp',
      description: 'Disconnect the workspace\'s WhatsApp pairing. Deletes the Evolution instance and clears the pairing row. Use to re-pair against a different phone.',
      scopes: ['whatsapp:write'],
      isWrite: true,
      requestExample: `curl -X POST \
  "${API_BASE_URL}/whatsapp/disconnect" \
  -H "Authorization: Bearer YOUR_API_KEY"`,
    },
  ],
};
